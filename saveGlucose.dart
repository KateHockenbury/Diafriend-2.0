/*import 'package:flutter/material.dart';

abstract class SaveGlucose extends StatelessWidget {
  late String glucoseLevel;
  WelcomePage({required this.glucoseLevel});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: Text('Glucose Level: $glucoseLevel'),
      ),
    );
  }
}
*/